import {PosterLinkPipe} from './poster-link.pipe';

describe('PosterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new PosterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
